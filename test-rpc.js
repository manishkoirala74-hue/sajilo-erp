import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.VITE_SAJILO_APP_BASE_URL, process.env.VITE_SAJILO_APP_ID); async function test() { const { data, error } = await supabase.rpc('get_comparative_profit_loss_rpc', { p_company_id: '00000000-0000-0000-0000-000000000000', p_from_date: '2026-01-01', p_to_date: '2026-12-31', p_comp_from_date: '2025-01-01', p_comp_to_date: '2025-12-31' }); console.log('Error:', error); console.log('Data:', data); } test();
