import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.VITE_SAJILO_APP_BASE_URL, process.env.VITE_SAJILO_APP_ID); async function run() { const { data } = await supabase.from('ChartOfAccount').select('account_name, account_type, account_subtype').limit(100); console.log(data); } run();
